{
  "name": "my1rm",
  "version": "0.1.0",
  "private": true,
  "description": "Static Cloudflare Pages MVP for squat, bench, and deadlift 1RM estimates.",
  "scripts": {
    "build": "node scripts/build-public.mjs",
    "check": "npm run build && node --check app.js && node --check functions/api/location.js && node --input-type=module --check < functions/api/events.js && node --input-type=module --check < functions/api/rank.js && node tests/calculator.test.js && node tests/analytics.test.mjs && node tests/rank.test.mjs"
  }
}
